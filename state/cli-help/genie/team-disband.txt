Usage: genie team disband [options] <name>

Disband a team (archives — preserves data). Use `genie team archive` directly.

Options:
  -h, --help     display help for command

Global Options:
  -V, --version  output the version number
